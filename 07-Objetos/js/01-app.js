const nombre = 'Monitor 20 pulgadas';  
const precio = 300;
const disponible = true;

// Un objeto es una agrupación de propiedades y valores, es una forma de almacenar datos relacionados entre sí.

// Object literal
const producto = {
    nombre: 'Monitor 20 pulgadas',
    precio: 300,
    disponible: true
}

console.log(producto);