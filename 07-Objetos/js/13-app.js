const producto = {
    nombre: 'Monitor 20 pulgadas',
    precio: 300,
    disponible: true
}

console.log(Object.keys(producto)); // Devuelve un array con las propiedades del objeto

console.log(Object.values(producto)); // Devuelve un array con los valores de las propiedades del objeto

console.log(Object.entries(producto)); // Devuelve un array con los pares clave-valor del objeto