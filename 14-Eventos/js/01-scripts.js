console.log(1);

// Se ejecuta el codigo una vez que todo el documentos haya cargado
document.addEventListener('DOMContentLoaded', () => {

    console.log('Documento listos');
    
});

console.log(3);