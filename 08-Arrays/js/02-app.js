const numeros = [10, 20, 30, 40, 50, [1, 2, 3]];
console.log(numeros);
console.table(numeros);

// Acceder a un valor específico del arreglo
console.log(numeros[0]);
console.log(numeros[5][1]); // Acceder al número 2 del arreglo anidado