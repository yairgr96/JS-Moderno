const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo'];

console.table(meses);

console.table(meses[0]);

// Cuanto mido el arreglo, me dice cuantos elementos tiene
console.log(meses.length);

for(let i = 0; i < meses.length; i++) {
    console.log(meses[i]);
}

