const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // Esto es una funcion 
// porque se ejecuta de forma independiente, no a traves de un objeto

console.log(numero1.toString()); // Esto es un metodo, porque se 
// ejecuta a traves de un objeto, en este caso el numero1